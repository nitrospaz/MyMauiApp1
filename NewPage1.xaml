<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.NewPage1"
             xmlns:resources="clr-namespace:MauiApp1.Resources"
             Title="NewPage1">
    <!-- This is the xaml that displays the controls and content, similar to a .NET Framework or .NET Core WPF application -->
    <!-- I think of it as the css for a web page and the work is done in the .cs file -->
    <VerticalStackLayout>
        <Label 
            Text="Count: 0"
            VerticalOptions="Center" 
            HorizontalOptions="Center" 
            TextColor="{x:Static Member=resources:SharedResources.FontColour}"/>
    </VerticalStackLayout>
</ContentPage>